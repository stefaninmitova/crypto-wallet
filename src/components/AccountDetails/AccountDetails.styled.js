import { Card } from "@mui/material";
import { styled } from "styled-components";

export const AccountDetailsCard = styled(Card)`
  display: flex;
  align-items: center;
  padding: 12px;
  gap: 12px;
`;
